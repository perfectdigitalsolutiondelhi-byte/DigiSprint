import type { Metadata } from "next";import { AppShell } from "../../components/layout/AppShell";import { StrategyGenerationForm } from "../../components/strategy/StrategyGenerationForm";import { StrategyHistory } from "../../components/strategy/StrategyHistory";import { requireStrategyWorkspace } from "../../lib/marketing-strategy/authorization";import { loadStrategyHistory } from "../../lib/marketing-strategy/queries";
export const metadata:Metadata={title:"AI Marketing Strategy",description:"Generate and review your business marketing strategy.",robots:{index:false,follow:false}};export const dynamic="force-dynamic";
export default async function StrategyPage(){const {supabase,user,business}=await requireStrategyWorkspace();const history=await loadStrategyHistory(supabase,business.id);return <AppShell userName={typeof user.user_metadata.full_name==="string"?user.user_metadata.full_name:null} userEmail={user.email} businessName={business.name} businessType={business.industry}><div className="dashboard-content strategy-workspace"><section className="strategy-intro"><div><span className="strategy-label">AI Marketing Strategy</span><h1>Turn your business context into a focused growth plan.</h1><p>DigiSprint analyses your audience, channels and goals to create one actionable 30-day strategy.</p></div><div className="strategy-context"><span>Business context</span><strong>{business.name}</strong><small>{business.industry||"Small business"} · {[business.city,business.state].filter(Boolean).join(", ")||"India"}</small></div></section><StrategyGenerationForm/><StrategyHistory strategies={history}/></div></AppShell>}
