import Link from "next/link";import type { StrategySummary } from "../../lib/marketing-strategy/types";import { StrategyStatusBadge } from "./StrategyStatusBadge";
export function LatestStrategyCard({ strategy }: { strategy: StrategySummary | null }) { return <section className="overview-card latest-strategy-card"><div className="overview-card-heading"><div><span>AI strategy</span><h2>{strategy?"Latest marketing strategy":"Strategy workspace"}</h2></div>{strategy&&<StrategyStatusBadge status={strategy.status}/>}</div>{strategy?<><strong>{strategy.title||`Strategy V${strategy.version}`}</strong><p>Version {strategy.version} · {new Intl.DateTimeFormat("en-IN",{dateStyle:"medium"}).format(new Date(strategy.createdAt))}</p><div><Link href={`/strategy/${strategy.id}`}>View Strategy</Link><Link href="/strategy">View History</Link></div></>:<><p>Generate a structured plan using your saved business context.</p><Link href="/strategy">Generate Marketing Strategy</Link></>}</section> }
