import Link from "next/link";import type { StrategySummary } from "../../lib/marketing-strategy/types";import { StrategyStatusBadge } from "./StrategyStatusBadge";
export function StrategyHistory({ strategies }: { strategies: StrategySummary[] }) { if(!strategies.length)return <div className="strategy-empty"><span>✦</span><h2>No strategies yet</h2><p>Your first complete marketing strategy will appear here after generation.</p></div>;return <section className="strategy-history"><div><span className="strategy-label">Saved work</span><h2>Strategy history</h2></div><div>{strategies.map(x=><Link href={`/strategy/${x.id}`} key={x.id}><span>V{x.version}</span><div><strong>{x.title||"Marketing strategy"}</strong><small>{new Intl.DateTimeFormat("en-IN",{dateStyle:"medium"}).format(new Date(x.createdAt))}</small></div><StrategyStatusBadge status={x.status}/><b>View →</b></Link>)}</div></section> }
