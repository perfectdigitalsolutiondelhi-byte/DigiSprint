import type { MarketingStrategyOutput } from "../../lib/marketing-strategy/types";import { StrategyList } from "./StrategyList";
export function AudiencePersonas({ data }: { data: MarketingStrategyOutput }) { return <section className="strategy-section"><span className="strategy-label">Customers</span><h2>Audience and personas</h2><div className="strategy-card-grid">{data.targetAudience.map(x=><article key={x.segment}><small>{x.priority}</small><h3>{x.segment}</h3><p>{x.description}</p></article>)}</div><div className="strategy-card-grid">{data.customerPersonas.map(x=><article key={x.name}><h3>{x.name}</h3><p>{x.profile}</p><h4>Needs</h4><StrategyList items={x.needs}/><h4>Objections</h4><StrategyList items={x.objections}/><p><strong>Best channels:</strong> {x.bestChannels.join(", ")}</p></article>)}</div></section> }
