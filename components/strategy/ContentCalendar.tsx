import type { MarketingStrategyOutput } from "../../lib/marketing-strategy/types";
export function ContentCalendar({ data }: { data: MarketingStrategyOutput["contentCalendar"] }) { return <section className="strategy-section"><span className="strategy-label">30-day plan</span><h2>Content calendar</h2><div className="calendar-table" role="table" aria-label="Thirty-day content calendar">{data.map(x=><article key={x.day} role="row"><strong>Day {x.day}</strong><span>{x.platform} · {x.format}</span><h3>{x.topic}</h3><p>{x.objective}</p><small>CTA: {x.callToAction}</small></article>)}</div></section> }
