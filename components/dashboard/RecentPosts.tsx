import type { DashboardPost } from "../../lib/dashboard/loadDashboard";

export function RecentPosts({ posts }: { posts: DashboardPost[] }) {
  return <section className="overview-card recent-posts"><div className="overview-card-heading"><div><span>Content activity</span><h2>Recent posts</h2></div><b>{posts.length ? `${posts.length} latest` : "No posts yet"}</b></div>{posts.length ? <div className="recent-post-list">{posts.map((post) => <article key={post.id}><div className="post-channel" aria-hidden="true">{post.platform.slice(0, 2).toUpperCase()}</div><div><strong>{post.title || "Untitled post"}</strong><p>{post.caption || "Caption not added yet."}</p></div><span className={`post-state ${post.status}`}>{post.status}</span><time dateTime={post.created_at}>{new Intl.DateTimeFormat("en-IN", { day: "numeric", month: "short" }).format(new Date(post.created_at))}</time></article>)}</div> : <div className="dashboard-empty"><span aria-hidden="true">+</span><h3>Your content library is ready.</h3><p>The daily post engine will use your saved business and brand preferences in the next product version.</p><small>No sample posts have been added to your account.</small></div>}</section>;
}
